export enum ESortBy {
  FIRST_NAME = "firstName",
  LAST_NAME = "lastName",
  AGE = "age",
  NATIONALITY = "nationality",
}

export enum ESortDir {
  ASC = "asc",
  DESC = "desc",
}

export enum ESearchParam {
  SEARCH = "search",
  NATIONALITY = "nationality",
  HOBBY = "hobby",
  SORT_BY = "sortBy",
  SORT_DIR = "sortDir",
  PAGE = "page",
  LIMIT = "limit",
}
