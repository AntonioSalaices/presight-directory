export enum ESortBy {
  FIRST_NAME = "first_name",
  LAST_NAME = "last_name",
  AGE = "age",
  NATIONALITY = "nationality",
}

export enum ESortDir {
  ASC = "asc",
  DESC = "desc",
}

export enum ESearchParam {
  SEARCH = "search",
  NATIONALITY = "nationality",
  HOBBY = "hobby",
  SORT_BY = "sortBy",
  SORT_DIR = "sortDir",
  PAGE = "page",
  LIMIT = "limit",
}
